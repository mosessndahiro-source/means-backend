<?php

return [
    'ride' => [
        'status_list' => ['pending', 'started', 'complete', 'cancelled', 'rejected'],
        'status_default' => 'pending'
    ],

    'rider' => [
        'status_list' => ['pending', 'accepted', 'picked', 'dropped', 'cancelled', 'rejected'],
        'status_default' => 'pending'
    ],

];

